/// <reference types="astro/client" />

// Injected by `vite.define` in astro.config.mjs (package.json version and
// the deployed commit); rendered in the footer.
declare const __SITE_VERSION__: string;
declare const __SITE_COMMIT__: string;
