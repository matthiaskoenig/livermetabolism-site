# Site settings
title: livermetabolism
email: koenigmx@hu-berlin.de
description: > # this means to ignore newlines until "baseurl:"
  LiSyM Junior Group König
baseurl: "" # the subpath of your site, e.g. /blog/
url: "https://www.livermetabolism.com" # the base hostname & protocol for your site
git_address: https://github.com/matthiaskoenig
git_edit_address: https://github.com/aksakalli/jekyll-doc-theme/blob/gh-pages
twitter_username: konigmatt
github_username:  matthiaskoenig



# theme options from https://bootswatch.com/
# comment out this to use default Bootstrap
bootwatch: flatly
# bootwatch: sandstone

# Build settings
markdown: kramdown
highlighter: rouge
plugins:
  - jekyll-feed
  - jekyll-redirect-from
  - jekyll-seo-tag
  - jekyll-sitemap

exclude:
  - Gemfile
  - Gemfile.lock
  - vendor
timezone: Europe/Berlin
defaults:
- scope:
    path: _posts
    type: posts
  values:
    layout: post
    sectionid: blog

- scope:
    path: _docs
    type: docs
  values:
    layout: docs
    sectionid: docs
    seo:
      type: "WebPage"

collections:
  docs:
    permalink: /:collection/:path/
    output: true
  posts:
    permalink: /blog/:year/:month/:day/:title/
    output: true
